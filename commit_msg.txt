feat: add github seeder endpoint

- Created temporary `GET /api/projects/seed-github` endpoint to automatically fetch repositories from the user's GitHub and insert them into the database
- Safely avoids duplicates by checking existing project names and GitHub URLs
- Auto-extracts tags, descriptions, category, GitHub links, and Live webapp URLs (from homepage)
