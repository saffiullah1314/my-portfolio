style: fix hero role text overflow layout

- Added `flex-wrap: wrap` to `TextLoop` to allow long roles to wrap cleanly
- Wrapped "I am a" in a nowrap span so it doesn't break into multiple lines when the flex container shrinks
- Centered the wrapped content nicely on mobile using `justify-content: center`
