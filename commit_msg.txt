fix: resolve mobile overflow bugs in Admin Dialogs

- Replaced rigid `minWidth: 500` and `minWidth: 520` in all Admin Dialog components with responsive `width: '100%', maxWidth: 600`
- This ensures that when adding new items (Projects, Experience, Education, Skills) on smaller screens or mobile devices, the popup dialog fits cleanly on the screen without causing horizontal overflow or breaking the layout
